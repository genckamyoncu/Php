# Go Base Balance Checker

Go dili ile Base ağındaki bir adresin ETH (wei) bakiyesini sorgular.

## Kurulum

```bash
go mod tidy
go run main.go
```

`.env` veya ortam değişkenleri:

```
BASE_RPC_URL=https://mainnet.base.org
ADDRESS=0x...
```
